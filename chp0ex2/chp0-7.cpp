#include <iostream> 
int main() {
    /* This is a multi-line comment
       because it uses the start and end delimiters /* and */ */
    std::cout << "Does this work?" << std::endl;
       return 0;
}
